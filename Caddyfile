{$HOST} {
    encode gzip

    route {
        handle /hasura* {
            uri strip_prefix /hasura
            reverse_proxy host.docker.internal:{$HASURA_PORT}
        }

        handle {
            reverse_proxy host.docker.internal:{$BACKEND_PORT}
        }
    }
}
